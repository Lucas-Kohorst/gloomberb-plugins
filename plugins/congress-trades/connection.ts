/**
 * Lives apart from index.tsx so the pane and detail views can report traffic
 * without importing the plugin definition that already imports them.
 */
export const CONGRESS_CONNECTION_ID = "congress-trades";
